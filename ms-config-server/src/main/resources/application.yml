server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          # ruta de tuuuu repositorio git :V
          uri: https://github.com/Anderson897lk/Examen22Abril.git
          searchPaths: config-data
          default-label: main
  security:
    user:
      #recordar este apartado para tus microservicios :V
      #Pon el usuario que desees
      name: JuanAnderson
      #pon la contraseña que desees
      password: 123456
